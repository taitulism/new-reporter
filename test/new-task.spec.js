const expect  = require('chai').expect;
const newTask = require('../index');

describe('newTask', () => {
    it('is a function', () => {
        const  = module();

        expect(newTask).to.be.a.function;
    });
});
